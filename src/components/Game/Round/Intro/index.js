import React, { useEffect } from "react";
import { Text } from "react-native";

import styles from "./style";

const index = ({ round }) => {
  return <Text>Le round {round} va commencer</Text>;
};

export default index;
